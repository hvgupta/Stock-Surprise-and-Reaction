# Oxbow Frontend

Temporary Next.js frontend template for the project.

## Run locally

```bash
npm install
npm run dev
```

The app currently shows a simple landing page while the backend is still being built.